version: '3'
services:
  client:
    build: ./client
    ports:
      - "81:7777"
    environment:
      - REDIS_HOST=redis
  redis:
    image: "redis:alpine"
  mongo:
    image: "mongo:latest"
  back:
    build: ./server
    ports:
      - "82:3000"
    environment:
      - PORT=3000
